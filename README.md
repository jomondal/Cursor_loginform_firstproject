# Login Form

My very first project, built with Cursor — a basic login form with client-side validation.

## Live Demo

**https://cursor-loginform-firstproject.vercel.app**

## Demo Accounts

| Username | Password |
|----------|----------|
| `admin` | `password123` |
| `demo` | `demo123` |

## Local Setup

Open `index.html` in a browser, or run a local server:

```bash
python -m http.server 8080
```

Then visit `http://localhost:8080`.
